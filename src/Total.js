import React from 'react'

const Total = (props) => {
    //return the length of the props list
    return (
        <div>
            Total number of names {props.sortedList.length}
        </div>
    )
}

export default Total 
